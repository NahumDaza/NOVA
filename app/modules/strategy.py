class StrategyModule:
    def think_process(self, message: str) -> str:
        return (
            "Vamos a estructurarlo con cabeza. Primero definimos objetivo, luego restricciones, "
            "después riesgos, y por último una secuencia operativa recomendada."
        )